FoamFile
{
    format      ascii;
    class       dictionary;
    location    "system";
    object      topoSetDict_Battery_pack_0;
}

actions
(
    {
        name    Battery_pack_0;
        type    cellSet;
        action  new;
        source  fieldToCell;
        field   heatSourceMask_0;
        min     0.5;
        max     1.5;
    }
    {
        name    Battery_pack_0;
        type    cellZoneSet;
        action  new;
        source  setToCellZone;
        set     Battery_pack_0;
    }
);
