count, sym_id = dev.GetSymbolIds()

print("count:", count)
print("sym_id:", sym_id)

sym.SetId(sym_id)

print("SetId result maybe:", sym.GetId() if hasattr(sym, "GetId") else "no GetId")
print("type:", sym.GetSymbolTypeName())


result = dev.GetSymbolIds()

print(result)
print(type(result))

if isinstance(result, tuple):
    for i, value in enumerate(result):
        print(i, value, type(value))

help(dev.GetSymbolIds)


