cur.execute("""
SELECT
    COPY,
    BundleID,
    ParentID,
    Name,
    NODENO,
    SUBENTRY,
    SUBVERSION
FROM dbo.Cable
WHERE ENTRY = ?
ORDER BY COPY
""", wire_lpntr)

for row in cur.fetchall():
    print(tuple(row))




cur.execute("""
SELECT LPNTR
FROM dbo.ComponentData
WHERE ENTRY = ?
AND ENTRYTYP = 22
""", "Twisted 5 Wire - 24AWG")

five_wire_lpntr = cur.fetchone()[0]

cur.execute("""
SELECT
    COPY,
    BundleID,
    ParentID,
    Name,
    NODENO,
    SUBENTRY,
    SUBVERSION
FROM dbo.Cable
WHERE ENTRY = ?
ORDER BY COPY
""", five_wire_lpntr)

for row in cur.fetchall():
    print(tuple(row))
