tables = [
    "Assembly",
    "ComponentAttribute",
    "ComponentData",
    "GatePinRelation",
]

cur = conn.cursor()

for table in tables:
    print("\n" + "=" * 80)
    print(table)
    print("=" * 80)

    # columns
    cur.execute("""
        SELECT COLUMN_NAME, DATA_TYPE
        FROM INFORMATION_SCHEMA.COLUMNS
        WHERE TABLE_SCHEMA='dbo'
          AND TABLE_NAME=?
        ORDER BY ORDINAL_POSITION
    """, table)

    cols = cur.fetchall()

    print("COLUMNS:")
    for c in cols:
        print(c)

    # newest rows - just grab a few for inspection
    cur.execute(f"""
        SELECT TOP (10) *
        FROM dbo.[{table}]
    """)

    print("\nROWS:")
    for row in cur.fetchall():
        print(row)
