def wait_for_e3_continue(message="Click somewhere in E3, then click Continue"):
    root=tk.Tk(); root.title("Continue E3 Script"); root.geometry("420x120")
    tk.Label(root,text=message,font=("Arial",11)).pack(pady=18)
    tk.Button(root,text="CONTINUE",width=30,command=root.destroy).pack()
    root.mainloop()



format_main_block_view(parent_block,xp,yp,xwidth,yheight)
time.sleep(0.5)
if i<len(PIN_SIGNAL_INFO)-1: wait_for_e3_continue(f"{conn_name} is complete.\nClick somewhere on the E3 sheet, then click CONTINUE.")


right before create view
dev.SetId(MAIN_DEVICE_ID); print("BEFORE CREATEVIEW:",i,MAIN_DEVICE_ID,dev.GetName()); res=dev.CreateView(MAIN_DEVICE_ID,1,0,1); print("CREATEVIEW RESULT:",res)
