TEST_ENTRY = "PUT_CONNECTOR_ENTRY_HERE"

cur.execute("""
SELECT
    ENTRY,
    PHYSPIN,
    PINNAME
FROM dbo.Pin
WHERE ENTRY = ?
""", TEST_ENTRY)

rows = cur.fetchall()

for row in rows:
    print(row)

print("ROWS:", len(rows))







