wire_lpntr = "LC-CD6DBA76-E2EB-4DBC-93AE-712B63B85274"

cur.execute("""
SELECT *
FROM dbo.Cable
WHERE ENTRY = ?
ORDER BY COPY
""", wire_lpntr)

columns = [col[0] for col in cur.description]
rows = cur.fetchall()

print("Rows:", len(rows))

for row in rows:
    print("\n" + "-" * 60)

    for name, value in zip(columns, row):
        print(f"{name} = {value}")



component_id = "ADB52ED8-D987-40A2-A8A2-28CFBC23A4A0"

cur.execute("""
SELECT *
FROM dbo.Cable
WHERE ENTRY = ?
ORDER BY COPY
""", component_id)

print("Rows:", len(cur.fetchall()))
