for child_id in device_ids.value:
    dev.SetId(child_id)

    print(
        "ID:", child_id,
        "| Name:", dev.GetName(),
        "| Component:", dev.GetComponentName(),
        "| Pins:", dev.GetPinCount(),
        "| Is assembly part:", dev.IsAssemblyPart()
    )
