[logger]
output_directory = "simulation_output"
enable_field_logging = true
enable_summary_logging = false
enable_probe_logging = false
field_interval = 1
field_variables = ["temperature", "pressure"]
