lpntr = "LC-14840D29-FF00-4442-B1BE-7C93C4894335"

cur.execute("""
SELECT
    COPY,
    NODENO,
    COPYEND1,
    PINCOPYEND1,
    COPYEND2,
    PINCOPYEND2
FROM dbo.WINRTYP_CONNECT_INFOS
WHERE LPNTR = ?
ORDER BY COPY, NODENO
""", lpntr)

print("WIRE   NODE   END1       END2")
print("-" * 50)

for copy, node, e1, p1, e2, p2 in cur.fetchall():
    print(
        f"{copy:<6} {node:<6} "
        f"COPY {e1} PIN {p1:<4} -> "
        f"COPY {e2} PIN {p2}"
    )
