print("component.Save():", component.Save())
print("job.Save(0):", job.Save(0))
print("app.Save(0):", app.Save(0))

time.sleep(2)

conn = pyodbc.connect(CONNECTION_STRING, autocommit=True)
cur = conn.cursor()

cur.execute("""
SELECT ID, ENTRY, VERSION, LPNTR
FROM dbo.ComponentData
WHERE ENTRY = ?
AND VERSION = ?
""", COMPONENT_NAME, COMPONENT_VERSION)

row = cur.fetchone()

print("ComponentData:", tuple(row) if row else None)

cur.close()
conn.close()
