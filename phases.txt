table = "WINRTYP_CONNECT_INFOS"

cur.execute("""
SELECT
    COLUMN_NAME,
    DATA_TYPE,
    IS_NULLABLE
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'dbo'
  AND TABLE_NAME = ?
ORDER BY ORDINAL_POSITION
""", table)

for r in cur.fetchall():
    print(r)

test_lpntr = "PUT_YOUR_TEST_COMPONENT_LPNTR_HERE"

cur.execute("""
SELECT *
FROM dbo.WINRTYP_CONNECT_INFOS
WHERE LPNTR = ?
""", test_lpntr)

before = set(cur.fetchall())

input("Now connect exactly ONE wire in DBE, save, then press ENTER...")

cur.execute("""
SELECT *
FROM dbo.WINRTYP_CONNECT_INFOS
WHERE LPNTR = ?
""", test_lpntr)

after = set(cur.fetchall())

print("\nNEW ROWS:")
for row in after - before:
    print(row)

print("\nREMOVED ROWS:")
for row in before - after:
    print(row)






name = "PYTHON_TEST_DELETE_ME_2"

cur.execute("""
SELECT ENTRY, VERSION, LPNTR
FROM dbo.ComponentData
WHERE ENTRY = ?
""", name)

for row in cur.fetchall():
    print(row)


