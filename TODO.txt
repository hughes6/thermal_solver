(
cd '/mnt/c/OpenFOAM/thermal_sim_SOLAR_ATB/SOLAR_ATB_20PCT_HEAT_LOAD.heated-flow-check' || exit

if test -s .heated_airflow_origin; then
    printf 'PREPARATION COMPLETE\n'
    cat .heated_airflow_origin
else
    printf 'PREPARATION INCOMPLETE — reconstruction log:\n'
    tail -n 40 reconstruct-donor.log
fi
)
