T=2.7399999999997262
for r in 0 1 2 3; do
  echo "===== processor$r ====="
  for f in U p T p_rgh phi rho; do
    if test -e "processor${r}/${T}/fluid/$f"; then
      echo "$f PRESENT"
    else
      echo "$f MISSING"
    fi
  done
done
